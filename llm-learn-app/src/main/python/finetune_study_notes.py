"""
Week7 Day1 第3步+第4步 —— LoRA核心数学直觉 + 微调面试高频5题
不跑代码，纯知识笔记，直接读就行
"""

# ============================================================
# 第3步：理解核心公式（20分钟）
# ============================================================

"""
LoRA核心数学（Section 4.1 直觉级理解）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 基础设定
   原权重矩阵 W ∈ R^{d×d}，参数量 = d²
   微调目标：找到 ΔW 使得 W' = W + ΔW 在下游任务上效果更好

2. LoRA分解
   ΔW = B × A
   - B ∈ R^{d×r}（上投影）
   - A ∈ R^{r×d}（下投影）
   - r << d（低秩约束）

   参数量 = 2 × r × d
   当 r=4, d=4096：
   - 全量参数 = 4096² = 16,777,216
   - LoRA参数 = 2×4×4096 = 32,768
   - 参数占比 = 0.2%！

3. 为什么有效？（论文核心洞察）
   "语言模型的适应能力是低秩的（rank-deficient）"
   
   论文实验证据：
   - 用GPT-3 175B测试，r=4 已经接近全量微调效果
   - A矩阵的奇异值衰减极快→说明ΔW确实可以用很低的秩近似
   - 预训练模型已经学好了90%的能力，微调只修10%的细节
   
   直觉类比：
   - 一幅画已经画好了90%，微调只是修细节→不需要重画整幅画
   - 微调的ΔW就像"补丁"，而不是"重写"

4. 缩放因子 α
   ΔW = (α/r) × B × A
   
   为什么需要α/r？
   - 不同rank r的ΔW范数不同，需要归一化
   - α/r 让不同rank的更新量量级一致
   - 通常设 α = r，此时 ΔW = B × A（不缩放）
   - α > r → 更强的调整（激进微调）
   - α < r → 更弱的调整（保守微调）

5. B初始化为零的关键设计
   - B = 0 → ΔW = B × A = 0
   - 训练第一步：W' = W + 0 = W（不破坏原始权重！）
   - 从"零扰动"开始学增量，比随机初始化更稳定
   - A用Kaiming初始化（正态分布）→保证梯度合理
   
   这就是为什么LoRA训练比全量微调更稳定的原因之一

6. 论文Figure 1（架构图）解读
   ┌─────────────────────┐
   │  Pre-trained W      │ ← 冻结，不更新
   │  (frozen weights)   │
   └─────────────────────┘
          │
          │ x (输入)
          ▼
   ┌──────┐     ┌──────┐
   │  W   │     │ B×A  │ ← LoRA旁路，可训练
   │(冻结) │     │(训练) │
   └──────┘     └──────┘
          │         │
          │    ×α/r │
          │         │
          ▼         ▼
          └─────────┤
                    │
                    ▼ h = Wx + (α/r)BAx
                    ↓ 输出
   
   关键：W和BA是并行计算的，不是串联！
   → 推理时可以把BA合并到W，变成 W' = W + (α/r)BA
   → 合并后只有一条路径，推理零开销！

7. 论文Table 1（r值对效果的影响）
   在GPT-3 175B上的WikiSQL任务：
   
   r=1:  WikiSQL acc ≈ 72%  （太低，欠拟合）
   r=2:  WikiSQL acc ≈ 78%  （开始有效）
   r=4:  WikiSQL acc ≈ 82%  （已经接近全量微调！）
   r=8:  WikiSQL acc ≈ 82.5%（进一步提升很小）
   r=16: WikiSQL acc ≈ 82.3%（开始过拟合，反而下降了）
   r=64: WikiSQL acc ≈ 80%  （严重过拟合）
   
   铁律：r=4-16通常够用，r太大过拟合，r太小欠拟合
   论文实验 r=4 已经接近全量微调效果！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"""


# ============================================================
# 第4步：记面试关键题（20分钟）
# ============================================================

"""
微调面试高频5题，直接记住答案

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Q1: "什么时候该微调而不是用Prompt/RAG？"

回答要点：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
┌─────────────────────────────────────────────┐
│          选择决策：微调 vs Prompt vs RAG       │
├──────────┬──────────┬──────────┬────────────┤
│ 场景      │ 微调      │ Prompt   │ RAG        │
├──────────┼──────────┼──────────┼────────────┤
│ 改模型行为 │ ✅最优    │ ❌不够    │ ❌无关     │
│ 改输出格式 │ ❌过重    │ ✅最优    │ ❌无关     │
│ 注入新知识 │ ✅可行    │ ❌上下文爆│ ✅最优     │
│ 风格/语气  │ ✅LoRA好  │ ✅可用    │ ❌无关     │
│ 实时数据   │ ❌跟不上  │ ❌跟不上  │ ✅最优     │
│ 复杂推理   │ ✅能改变  │ ✅CoT     │ ❌检索不够 │
└──────────┴──────────┴──────────┴────────────┘

一句话总结：
"需要改变模型内在行为/知识时微调；
 只需改变输出格式或检索知识时用Prompt/RAG"

面试加分话术：
"微调改变的是模型的'本能'，Prompt改变的是'指令'，
 RAG改变的是'知识来源'。三者解决的问题层次不同。"


Q2: "LoRA为什么能省参数？"

回答要点：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
核心公式：ΔW = B × A
- B ∈ R^{d×r}, A ∈ R^{r×d}, r << d
- 可训练参数 = 2rd（对比全量 d²）
- 当 r=4, d=4096 → 32,768 vs 16,777,216 → 省了99.8%

为什么有效（面试必说）：
"权重更新是低秩的——预训练模型已经学好90%的能力，
 微调只需要修10%的细节，不需要全量更新就能达到
 接近全量微调的效果。论文实验证明r=4已经够用。"

面试加分话术：
"LoRA的精妙设计：B初始化为零→ΔW=0→训练从零扰动开始，
 不破坏原始权重。这比Adapter方法多了'合并推理零开销'
 的优势——训练完把BA合并到W，推理和原模型一样快。"


Q3: "QLoRA比LoRA省在哪？"

回答要点：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
省显存，不是省参数！

┌─────────────┬──────────┬──────────┐
│ 维度          │ LoRA     │ QLoRA    │
├─────────────┼──────────┼──────────┤
│ 基模型存储    │ FP16     │ NF4(4bit)│
│ 基模型显存    │ 14GB(7B) │ 3.5GB    │
│ LoRA部分     │ FP16     │ FP16     │
│ LoRA参数     │ 相同      │ 相同      │
│ 训练梯度     │ FP16     │ FP16     │
│ 总显存(7B)   │ ~16GB    │ ~5GB     │
│ 压缩比       │ -        │ 4倍      │
│ 精度损失     │ 无       │ ~10%     │
└─────────────┴──────────┴──────────┘

核心机制：
"基模型用NF4量化压缩到4bit（省4倍显存），
 训练时反量化回FP16计算前向传播，
 梯度只反传到LoRA适配器（LoRA部分还是FP16精度），
 基模型权重冻结不动。"

面试加分话术：
"QLoRA的三重优化：(1)NF4量化基模型省显存
 (2)分页优化器(Paged Optimizer)省梯度显存
 (3)双量化(Double Quantization)再省量化常量显存
 三招合打，4GB显存也能微调7B模型。"


Q4: "LoRA推理有额外开销吗？"

回答要点：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
无！零开销！

"训练完把 B×A 合并到 W：
 W' = W + (α/r) × B × A
 合并后推理路径 = 单矩阵乘法，和原模型完全一样"

对比其他方法的开销：
┌─────────────┬──────────────┐
│ 方法          │ 推理额外开销   │
├─────────────┼──────────────┤
│ 全量微调      │ 无            │
│ LoRA(合并后) │ 无！✅        │
│ LoRA(未合并) │ 两路并行+叠加 │
│ Adapter      │ 有串联延迟    │
│ Prefix-tuning│ 有前缀占用    │
└─────────────┴──────────────┘

面试加分话术：
"这是LoRA比Adapter方法最大的优势——Adapter在模型中
 插入额外层，推理有串联延迟；LoRA是旁路结构，
 合并后完全消除，推理和原模型一样快。"


Q5: "LoRA的rank r怎么选？"

回答要点：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
r=4-16通常够用

论文实验数据(GPT-3 175B WikiSQL)：
- r=1:  ≈72%（欠拟合）
- r=4:  ≈82%（接近全量微调！✅推荐起点）
- r=8:  ≈82.5%（进一步提升很小）
- r=16: ≈82.3%（开始下降）
- r=64: ≈80% （严重过拟合）

选择策略：
┌──────────────┬─────────┐
│ 场景           │ 推荐 r  │
├──────────────┼─────────┤
│ 风格/语气调整  │ r=4     │
│ 领域适配       │ r=8     │
│ 任务类型转换   │ r=16    │
│ 数据量<1k     │ r=4-8   │
│ 数据量>10k    │ r=8-16  │
│ 消费级GPU      │ r=4(省显存)│
│ A100/H100     │ r=8-16  │
└──────────────┴─────────┘

面试加分话术：
"r太大过拟合，r太小欠拟合，r=4是稳妥的起点。
 实际项目中建议从小r开始试，逐步增大观察效果拐点。
 另外α/r的缩放也很重要——α>r意味着更强的调整力度。"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

面试万能收尾话术：
"微调不是万能的——大多数场景先用Prompt Engineering
 和RAG尝试，只有确实需要改变模型内在行为时才微调。
 微调时优先LoRA（性价比最高），只有GPU极度受限才
 考虑QLoRA（牺牲一点精度换显存），有A100集群+极致
 效果需求才考虑全量微调。"
"""

print("📖 Week7 Day1 第3步+第4步 学习笔记")
print("上面是完整的数学直觉 + 面试5题")
print("直接读代码注释即可，无需运行")
print()
print("快速回顾：")
print("  第3步核心 → ΔW = (α/r)×B×A, B零初始化, r=4够用, 合并推理零开销")
print("  第4步面试 → 5题5答+加分话术+万能收尾")
